/* Write two functions - countBs (takes a string argument and returning a count of the capital Bs in string) as & a variation, countChar, that takes another argument - what character is being found and summed.
 */

// var string is the only input

function countBs(var string) {
  console.log("Input a string: ");
}

function countChar(var string, var characterSummed) {
  console.log("Input a string and the character you want to count: );
}
